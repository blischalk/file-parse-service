(ns file-parse-service.core-spec
  (:require [speclj.core :refer :all]
            [file-parse-service.core :refer :all]))

(describe "Truth"
  (it "is true"
    (should true))
  (it "is not false"
    (should-not false)))
